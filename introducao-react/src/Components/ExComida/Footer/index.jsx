import "./Footer.css"

const Footer = (props) => {
    
    return (
    
        <footer className="footerStyle">
            <h1 className="titleStyle">{props.title}</h1>
        </footer>

    )
}
export default Footer