const times = ['Palmeiras', 'Corinthians', 'São Paulo', 'Santos'];

//Usando o loop for para percorrer o array      
//lenghth é uma propriedade que retorna o tamanho do array
for (let i = 0; i < times.length; i++) {
    console.log("Time: " + i + " - " + times[i]);
}